portals:
  - city: Example City
    url: https://data.example.gov
    type: ckan
    notes: "Replace with a real CKAN portal definition."
  - city: Example Hub
    url: https://example.opendata.arcgis.com
    type: arcgis
    # Optional. `notes` is internal-only. `warning` is appended to the MCP
    # server description shown to users (e.g. flag incomplete coverage).
    warning: "This portal is incomplete — dataset coverage is partial."
    notes: "Replace with a real ArcGIS Hub portal definition."
  - city: Example Socrata
    url: https://data.example.gov
    type: socrata
    notes: "Socrata portals require an app token at deploy time."
