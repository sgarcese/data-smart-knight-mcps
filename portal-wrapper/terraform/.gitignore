# Terraform state (may contain secrets and rendered config)
*.tfstate
*.tfstate.*
.terraform/
crash.log
crash.*.log
terraform.tfplan
*.tfplan

# Local backend config and variable files holding secrets
backend.s3.hcl
*.tfvars
*.tfvars.json
!*.tfvars.example

# Build and generated artifacts
build/
generated-configs/
*.zip

# Keep the provider lock file committed (.terraform.lock.hcl is intentionally tracked)
